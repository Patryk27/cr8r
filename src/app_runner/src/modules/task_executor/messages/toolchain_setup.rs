/*
debug!("Setting up toolchain.");

        Command::new("/snap/bin/lxc")
            .args(&["exec", &self.container_name(), "--", "bash", "-c", "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y"])
            .output()
            .unwrap();

        debug!("Toolchain ready.");
*/