syntax = "proto3";

package cr8r.core;

message PExperimentDef {
    message PTryToolchain {
        string toolchain = 1;
    }

    //    message PTryPatch {
    //        // @todo
    //    }

    oneof op {
        PTryToolchain try_toolchain = 1;
    }
}