syntax = "proto3";

package cr8r.controller.runners.find;

import "../../core.proto";

message Request {
    //
}

message Reply {
    repeated Runner runners = 1;
}

message Runner {
    string name = 1;
    RunnerStatus status = 2;
}

enum RunnerStatus {
    IDLE = 0;
    WORKING = 1;
}