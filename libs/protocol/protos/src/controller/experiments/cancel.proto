syntax = "proto3";

package cr8r.controller.experiments.cancel;

import "../../core.proto";

message Request {
    string id = 1;
}

message Reply {
    oneof reply {
        SuccessReply success = 1;
        core.Error failure = 2;
    }
}

message SuccessReply {
    //
}